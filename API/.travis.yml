  language: python
  
  branches:
    only:
      - develop

  python:
    - "3.6"

  install:
    - pip install -r requirements.txt
  
  env: 
    - APP_SETTINGS='testing'
  script:
    - pytest

  

  